PROJECT(AbriToSotWriter)
INCLUDE(${PARAVIEW_USE_FILE})

FIND_PACKAGE(Qt4)
IF(QT4_FOUND)

INCLUDE(${QT_USE_FILE})

FILE(GLOB CLIPS_FILES_CPP "${CMAKE_CURRENT_SOURCE_DIR}/CLIPS/*.c")
FILE(GLOB CLIPS_FILES_H "${CMAKE_CURRENT_SOURCE_DIR}/CLIPS/*.h")

FILE(GLOB CLIPS_INTEGRATION_C "${CMAKE_CURRENT_SOURCE_DIR}/CLIPS/Integration/*.c")
FILE(GLOB CLIPS_INTEGRATION_CPP "${CMAKE_CURRENT_SOURCE_DIR}/CLIPS/Integration/*.cpp")
FILE(GLOB CLIPS_INTEGRATION_H "${CMAKE_CURRENT_SOURCE_DIR}/CLIPS/Integration/*.h")

SET( CLIPS_SOURCES
	${CLIPS_FILES_CPP}
	${CLIPS_FILES_H}
	
	${CLIPS_INTEGRATION_CPP}
	${CLIPS_INTEGRATION_C}
	${CLIPS_INTEGRATION_H}
	)
INCLUDE_DIRECTORIES(
${VTK_INCLUDE_DIR}
${PARAVIEW_INCLUDE_DIRS}
${ParaView_SOURCE_DIR}/VTK/GUISupport/Qt
${pqComponents_SOURCE_DIR}
${pqComponents_BINARY_DIR}
${pqCore_SOURCE_DIR}
${pqCore_BINARY_DIR}
${QtWidgets_SOURCE_DIR}
${QtWidgets_BINARY_DIR}
${QtChart_SOURCE_DIR}
${QtChart_BINARY_DIR}
${QtTesting_SOURCE_DIR}
${QtTesting_BINARY_DIR}
${QtPython_SOURCE_DIR}
${QtPython_BINARY_DIR}
../../../../Stable/Filters/SharedCode
../../../../Stable/Filters/
)
 
ADD_PARAVIEW_PLUGIN(AbriToSotWriterCPlugin "1.0"
	GUI_RESOURCES AbriToSotWriter.qrc)

ADD_PARAVIEW_PLUGIN(AbriToSotWriterSMPlugin "1.0"
	SERVER_MANAGER_XML AbriToSotWriter.xml
	SERVER_MANAGER_SOURCES vtkAbriToSotWriter.cxx
	SERVER_SOURCES ${CLIPS_SOURCES}
        SOURCES ../../../../Stable/Filters/SharedCode/TimeConverter.cxx )

ENDIF(QT4_FOUND)


#CPACK CODE TO BUNDLE THE DLLS CORRECTLY
INSTALL(TARGETS AbriToSotWriterCPlugin AbriToSotWriterSMPlugin
	RUNTIME DESTINATION bin/plugins COMPONENT Runtime
	LIBRARY DESTINATION bin/plugins 
	ARCHIVE DESTINATION bin/plugins 
)