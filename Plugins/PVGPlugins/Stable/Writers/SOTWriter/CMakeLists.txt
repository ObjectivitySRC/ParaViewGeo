PROJECT(SOTWriter)
FIND_PACKAGE(ParaView REQUIRED)
INCLUDE(${PARAVIEW_USE_FILE})

ADD_PARAVIEW_PLUGIN(SOTWriterClientPlugin "1.0"
	GUI_RESOURCES SOTWriter.qrc)

ADD_PARAVIEW_PLUGIN(SOTWriterServerManagerPlugin "1.0"
	SERVER_MANAGER_XML SOTWriter.xml
	SERVER_MANAGER_SOURCES vtkSOTWriter.cxx)